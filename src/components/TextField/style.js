const root = {
  display: 'flex',
  flexDirection: 'column',
};

const child = {
  padding: '10px',
  borderWidth: '2px',
  borderStyle: 'solid',
  borderColor: 'black',
  borderRadius: '5px',
};

export { root, child };
